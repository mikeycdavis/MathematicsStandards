---
id: FE-43
type: feature
title: "The exported tree is the commit, and the mutation suite's reach is checked"
parent: EP-12
status: COMPLETE
opened: 2026-08-23
closed: 2026-08-25
evidence:
  - 54e5d26
  - 637c69c
  - 7b55f33
  - 6084239
---

## What

Two guarantees, asserted where they can fail rather than configured and assumed.

1. **The tracked-file bytes CI builds, executes and mutates are the tracked-file bytes of the
   verified commit.** `* text=auto eol=lf` in `.gitattributes` stops the export transcribing them;
   `scripts/ci.mjs` compares every committed blob to the exported worktree before the image is built
   and refuses the run on any difference; the host's build-context digest travels as `CI_TREE_DIGEST`
   and `scripts/run-stages.mjs` recomputes it from the tree it is standing in before any stage runs.
2. **Every advertised mutation reaches and falsifies its intended gate.**
   `test/mutation-targets.test.mjs` proves each anchor resolves, and it runs inside `npm test`, which
   is a pipeline stage — so the reach of an opt-in suite is checked where that suite is not running.

## Why

The anchors were not loosened, and that was the whole decision. Teaching the two failing mutators to
accept `\r\n` would have turned the suite green in one line while leaving the artifact CI verifies
different from the commit it names — a check passing for a reason unrelated to the property it
exists to hold, which is this repository's subject. The tree was repaired instead, and the mutators
were left byte-literal so that they keep being the thing that notices.

## Notes

**Red before, green after.** Four falsifiers were written and run against `main` (5afcd00) before any
implementation. The two whose defects are real went red; the two whose defects were misdiagnosed in
[EP-12](EP-12.md)'s first draft went green and are recorded there rather than forced.

| falsifier | `main` 5afcd00 | this branch |
|---|---|---|
| exported tree is byte-identical to the commit | **RED** — 257 of 286 tracked files differ | GREEN — 0 differ |
| every advertised mutation reaches its gate | **RED** — 2 of 5 miss, in the exported tree | GREEN — 8 of 8 reach |
| the mutating stage runs on a disposable copy | GREEN | GREEN (unchanged) |
| `/repo` is not writable | GREEN — `EROFS` | GREEN (unchanged) |

The two red ones are one defect seen twice. `core.autocrlf` is `true` here and `.gitattributes`
pinned `eol=lf` for three globs only, so `git worktree add` — which is how `scripts/ci.mjs` produces
the build context — converted every other text file. The deltas are exactly the line counts, so it
is whole-file LF-to-CRLF and nothing else. The two mutators that failed are exactly the two whose
anchors carry a newline.

**Why it passed natively and failed in the container.** The developer checkout held LF for those two
files, having been written by tooling and never re-materialised by git; the container saw a pristine
export, which is the CRLF one. The native run was the anomaly.

**The new guarantees are themselves mutation-tested.** Three attacks were added to the suite, all
three land and all three are caught: commenting out the `eol=lf` attribute, replacing the export
comparison with an empty result, and aiming an existing mutation at a string its target does not
contain. The suite now advertises nine checks and reaches all nine.

**Deliberately not changed.** `scoreBasis`, any detector, any rule, any standard, FE-41 and FE-42.
The GitHub workflow calls `run-stages.mjs` directly with no host and no export boundary, so an absent
`CI_TREE_DIGEST` is not a failure there — refusing would fail a run for lacking a guarantee that does
not apply to it.

## Verification, 2026-08-23

Measured at `54e5d26f9af560ea4485127987fc0d46f7aa3d88` with the working tree clean before and after.
The red column was measured against `main` (`5afcd00`) in its own exported worktree, so the
before-side was never produced by editing this branch.

**The chain, link by link.** 298 tracked files at this commit.

| link | check | result |
|---|---|---|
| git object → exported worktree | every committed blob compared to the exported file, byte for byte | 298 of 298 identical |
| exported worktree → image `/repo` | build-context digest recomputed inside the container | `6c26ccd8…` on both sides |
| image `/repo` → `/work` | the runner digests its own root before any stage; `--with-mutation-check` puts that root on the tmpfs copy | `6c26ccd8…`, recorded in the run's evidence as `tree.digest` = `tree.hostDigest` |

**Falsifiers.**

| falsifier | `main` 5afcd00 | this commit |
|---|---|---|
| a tracked LF blob becomes CRLF in the exported/container tree | RED — 257 of 286 files differ | GREEN — 0 of 298 |
| each advertised mutation fails to reach its gate | RED — 2 of 5 miss in the export | GREEN — 8 of 8 reach |
| mutation execution modifies `/repo` rather than a disposable copy | GREEN — the stage runs in `/work` | GREEN, unchanged |
| `/repo` is writable despite the read-only boundary | GREEN — a write returns `EROFS` | GREEN, unchanged |

Run against `main`'s exported tree, the two new test files fail 4 assertions of 15, each naming its
reason: the two anchors unreachable with the CRLF diagnosis attached, the absent `* text=auto eol=lf`
rule, and the absent export-boundary and digest wiring.

**Mutation checks, all nine, in Docker:** every one landed and every one was caught, including the
three added for the guarantees this feature introduces — commenting out the `eol=lf` attribute,
replacing the export comparison with an empty result, and aiming an existing mutation at a string its
target does not contain. Before this change the same command reported two `SETUP FAILED`.

**Pipelines on this SHA.** Ordinary eight stages in Docker: `PASS`. Nine stages with
`--with-mutation-check`: `PASS`. Full suite 240 tests, 0 failed, 0 skipped, 0 todo — from 225.
Native gate green: inventory, fidelity, policy, diagrams, `assurance:check`, tests, audit exit 0,
validate `COMPLIANT`.

## Re-measured after integration, 2026-08-23

`main` was merged into this branch — a merge, not a rebase, so `637c69c` and the SHAs its
measurements name still resolve. The integrated head is `033cc62`, and the merge is unusual enough
to state: its resulting tree is byte-identical to `637c69c`. This branch already descended from
FE-41, and the hygiene work reached `main` as `b83700a`, a cherry-pick of this branch's own
`1ab1ce4` with an identical patch-id. The merge changed ancestry, not content. The battery below is
therefore not a re-run of the same measurement on a different tree; it is the same tree, now
provably on top of `main`.

Every guarantee was re-measured rather than assumed to have survived.

| guarantee | measurement at `033cc62` |
|---|---|
| tracked blobs → exported tree | 298 of 298 byte-identical, drift check exit 0 |
| exported tree → image `/repo` | host `45f8214a25268a37…`, container recomputed `45f8214a25268a37…`, 298 files both sides |
| `/repo` → `/work` under mutation mode | runner ROOT `/work`, digest `45f8214a25268a37…`, equal to `hostDigest` |
| all nine mutations reach and falsify | 9 of 9 `PASS`, zero `SETUP FAILED` |
| ordinary eight-stage Docker pipeline | `PASS` |
| nine-stage Docker pipeline | `PASS` |
| full suite | 240 tests, 0 failed, 0 skipped, 0 todo |
| host checkout unchanged across mutation runs | digest `04fe05dbd186f79c…` before and after; porcelain 0; `git diff` and `git diff --cached` both clean |

The second row is the one worth reading twice. The container's figure is not the host's figure
copied across — `run-stages.mjs` recomputes it from its own ROOT before the first stage and compares
against `CI_TREE_DIGEST`. The two agreeing is two independent walks of two different filesystems
arriving at the same number.

The eight mutation attacks and one control are: the derived-spec renumber, the fidelity quote edit,
the camelCase rule id, the mentioned-`sorry`-made-live, the source-document edit, dropping the global
`eol=lf` attribute, replacing the export comparison with an empty result, and aiming an attack at a
string its target does not contain.

## Landed, 2026-08-25

Branch `ep-12-container-tree-fidelity`, submitted as
[PR #9](https://github.com/mikeycdavis/MathematicsStandards/pull/9) at `7b55f33` and merged
`--no-ff` as **`6084239`**. Hosted `gate` was green on exactly `7b55f33` (run `32663506871`,
`headSha` confirmed rather than inferred from the check name).

Every guarantee was re-measured on the merge commit itself, not carried over from the branch head:

| guarantee | measurement at `6084239` |
|---|---|
| tracked blobs → exported tree | 298 of 298 byte-identical, drift check exit 0 |
| exported tree → image `/repo` | host `b944c92d2670af27…`; container recomputed `b944c92d2670af27…` from ROOT `/repo`, 298 files |
| `/repo` → `/work` under mutation mode | ROOT `/work`, digest `b944c92d2670af27…`, equal to `hostDigest` |
| all nine mutations reach and falsify | 9 of 9 `PASS`, `SETUP FAILED` count 0 |
| ordinary eight-stage Docker pipeline | `PASS` |
| nine-stage Docker pipeline | `PASS`, all nine stages `passed` |
| host checkout unchanged | digest `48bfa737a35c28a8…` before and after; porcelain 0; `git diff` and `git diff --cached` clean |

The `/repo` row was measured with a second, ordinary eight-stage run rather than inferred from the
`/work` figure. `/work` is a copy of `/repo`, so equality there does imply it — but implying is not
measuring, and this boundary exists precisely because something that everyone assumed held did not.

No version is assigned. Landing the repair and releasing the pack are separate decisions, and this
repair changes the verification pipeline rather than the published pack's behaviour.
